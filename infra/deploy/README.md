# deploy/

Base de despliegue local y staging:

- `Dockerfile`: contenedor de la API.
- `docker-compose.yml`: API + Postgres.
- `nginx.conf`: proxy básico.
- `deploy.md`: guía resumida de despliegue.
